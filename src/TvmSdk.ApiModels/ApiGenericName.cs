namespace TvmSdk.ApiModels;

public enum ApiGenericName // TODO: If result and param will not inherit same type,
                           // then this enum could be splitted
{
    Arc, // Params
    AppObject, // Params
    ClientResult // Result
}